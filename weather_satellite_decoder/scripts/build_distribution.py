from setuptools import setup, find_packages

setup(
    name='my_project',
    version='1.0',
    packages=find_packages(),  # Automatically find all packages under the 'my_project' directory
    # Other setup options...
)
